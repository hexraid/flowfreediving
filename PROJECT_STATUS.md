# PROJECT_STATUS.md

## 1. 현재 작업 기준
- **현재 작업 폴더명**: `flow_backup_14_관리자재작업시작`
- **작업 기준 및 배경**:
  - 본 작업 폴더는 `flow_backup_13_설정기본SEOFooter완료`를 복사하여 생성한 관리자 재작업 시작본입니다.
  - 기존 백업 폴더(`flow_backup_13_설정기본SEOFooter완료`)는 원본 그대로 안전하게 보존하며, 앞으로의 모든 수정 및 기능 구현 작업은 `flow_backup_14_관리자재작업시작` 폴더 내에서 진행합니다.

---

## 2. 현재 관리자 및 홈페이지 기능 상태

### [관리자 페이지 메뉴별 구현 상태]
1. **대시보드 (`dashboard`) [완료]**:
   - 대시보드 UI 개선 완료 (시안 1:1 기반 프리미엄 카드 레이아웃)
   - 방문자 통계 4종 (오늘/이번 주/전체 방문자, 페이지뷰): 실제 GA 연동 전 0 표시 및 API 연동 DOM 구조 유지
   - 최근 수정 내역: 최근 5개 고정 표시, 관리자명(`admin`) 대신 작업 관련 관리 메뉴명 태그 표시 (클릭 시 해당 메뉴 즉시 이동)
   - 빠른 작업 4종 카드 (교육과정 추가, 팝업 추가, FAQ 추가, 후기 추가) 단일 라우팅 연동 (진입 후 브라우저 뒤로가기 시 대시보드로 즉시 복귀)
   - 연동 서비스 3종 현황 표시 (네이버 예약, 카카오톡 상담, 스마트스토어)
   - 알림 및 공지: 실데이터 기반 현재 노출중 팝업 수, 노출 기간, D-Day 계산(`종료 D-N`, `오늘 종료`) 표시 및 클릭 시 팝업 관리/수정 화면 이동
   - 대시보드에서 불필요한 전체보기/펼치기/모달 기능 제거 (간결하고 안정적인 컴팩트 UI 유지)
   - 시스템 상태 및 Danger Zone UI 대시보드 제거 (JS 백업/복원 로직은 100% 안전 보존)
   - 한 화면 중심의 컴팩트 레이아웃 적용 및 세로 스크롤 정상 허용

2. **교육과정 (`programs`) [완료]**:
   - 목록 조회: 실시간 검색(과정명/소제목/설명/태그/커리큘럼 등 다중 필드 검색), 카테고리/공개여부 필터링, 3종 정렬(노출순/이름순/가격순), `+ 교육과정 추가` 버튼 명칭 통일, `순서` 열 줄바꿈 없는 가로 1줄 및 너비 최적화
   - 마우스 드래그 앤 드롭을 통한 노출 순서 변경 및 "순서 적용하기" 버튼 연동 (LocalStorage 영구 저장)
   - 상세 에디터 (데스크톱 1920×1080 1화면 중심 컴팩트 UI):
     - 상단 액션바: `[← 교육과정 목록] | [과정명]`, `[👁️ 미리보기]`, `[📄 교육과정 복제]`, `[💾 저장하기]`(파란색 Primary 버튼) 순서 배치 및 저장 연동
     - 기본 정보 + 대표 이미지: 과정명/카테고리/소제목/수강료/핵심설명/공개여부 2열 배치 + 정사각형(`155×155px`) 대표 이미지 프리뷰 및 우측 슬림 세로 버튼(`140px`) 정렬(업로드/변경/삭제)
     - 교육 내용: 4열 한 줄 배치 (`상세 커리큘럼`, `포함 사항`, `개인 준비물`, `주의사항` - 동일한 균일 높이 `120px`로 스크롤 없이 확인 가능)
     - 판매 및 노출 설정 (통합 카드): `스마트스토어 개별 링크` | `태그` (Tag Chip UI)
     - 교육과정 삭제: 박스/설명/배경 제거 및 우측 하단 단독 빨간색 버튼 배치 (상단과 10px 자연스러운 여백 확보, 기존 안전 확인 모달 및 삭제 로직 100% 보존)
     - 상세 화면 내 개별 SEO 설정 완전 삭제 (사이드 메뉴 전체 SEO 설정은 보존)

3. **FAQ (`faq`) [완료]**:
   - 질문(Question) 및 답변(Answer) 관리자 및 메인 홈페이지 연동
   - [관리자] 실제 등록된 FAQ 개수 자동 계산 및 상단 뱃지(`FAQ 목록 N`) 동적 표시
   - [관리자] 한 페이지당 10개 단위 페이지네이션 적용 (10개 이하 자동 숨김, 11개 이상부터 `< 1 2 ... >` 컨트롤 활성화, 최대 5개 슬라이딩 윈도우)
   - [관리자] 새 FAQ 등록, 상세 편집, 확인 모달 기반 삭제(교육과정과 동일한 통합 안전 모달 연동) 및 추가/삭제 시 자동 페이지 및 카운트 동기화
   - [메인 홈페이지] FAQ 최초 6개 기본 노출 (6개 이하 시 버튼 숨김, 7개 이상 시 `[ FAQ 더보기 ↓ ]` 단독 버튼 표시)
   - [메인 홈페이지] `FAQ 더보기` 클릭 시 6개씩 추가 노출 (아직 남은 FAQ가 있는 경우 `[ FAQ 더보기 ↓ ]` 유지, 전체 노출 완료 시 `[ FAQ 접기 ↑ ]` 단일 버튼으로 전환)
   - [메인 홈페이지] `FAQ 접기 ↑` 클릭 시 초기 6개 상태로 복구 및 `#faq` 상단으로 부드러운 스크롤 이동, 버튼은 다시 `[ FAQ 더보기 ↓ ]`로 복원
   - [메인 홈페이지] 질문 클릭 시 답변 열림/닫힘 아코디언 인터랙션 100% 정상 유지

4. **후기 (`reviews`) [완료]**:
   - 목록 조회: 수강생명, 참가 과정, 후기 내용, 작성 날짜 조회
   - [목록] 실제 등록된 후기 개수 자동 계산 및 상단 뱃지(`후기 목록 N`) 동적 표시
   - [목록] 한 페이지당 10개 단위 페이지네이션 적용 (10개 이하 자동 숨김, 11개 이상부터 `< 1 2 ... >` 컨트롤 활성화, 최대 5개 슬라이딩 윈도우)
   - [상세 편집] 데스크톱 2열 컴팩트 레이아웃 (1행: 수강생명 \| 참가 과정, 2행: 후기 평점 \| 작성 날짜, 3행: 후기 내용)
   - [참가 과정] 기본 `프리다이빙` + 현재 등록된 교육과정 동적 연동 + `+ 직접 입력` 지원
   - [작성 날짜] Date Picker(`<input type="date">`) 적용 및 `YYYY-MM-DD` 표준 포맷 저장, 기존 데이터 호환
   - [삭제] 교육과정/FAQ와 동일한 안전 확인 모달 연동 및 실시간 목록/카운트/페이지네이션 자동 갱신

5. **강사 관리 (`instructors`) [완료]**:
   - 목록 조회: 순서(드래그 핸들 `⠿`), 사진, 강사명, 역할/소속, 교육 철학, 메인 노출 토글
   - [상태 통일] 복잡한 공개/비공개 상태를 제거하고 **`메인 노출` 단일 토글(ON/OFF)**로 통합
   - [순서 변경] 마우스 드래그 앤 드롭을 통한 강사 노출 순서 변경 및 "순서 적용하기" 버튼 연동 (LocalStorage 영구 저장 및 메인 홈페이지 순서 100% 동기화)
   - [목록 인터랙션] 불필요한 수정/삭제 버튼을 제거하고 **행 전체 클릭 시 상세 편집 화면으로 이동** (드래그 핸들 및 메인 노출 토글 클릭 시 이벤트 버블링 차단)
   - [페이지네이션] 한 페이지당 10명 단위 페이지네이션 적용 (10명 이하 자동 숨김, 11명 이상부터 `< 1 2 ... >` 컨트롤 활성화)
   - [상세 에디터] 프로필 사진 업로드/변경/삭제, `메인 노출` 단일 토글, 강사명, 소속/역할, 교육 철학, BIO, 자격증 및 이력/경력 멀티라인 파싱, 안전 확인 모달 기반 삭제 연동

6. **메인 Hero (`hero`) [완료]**:
   - [URL 입력 제거] URL 직접 입력창을 완전히 제거하고 순수 파일 업로드 방식으로 단순화 (URL은 내부 자동 저장)
   - [배경 영상] 등록 시 실제 `<video>` 재생 미리보기 지원 / 미등록 시 `등록된 영상 없음` 플레이스홀더 표시, `[영상 업로드]`, `[영상 변경]`, `[영상 삭제]` 상태별 버튼 연동
   - [배경 이미지] 등록 시 실제 `<img>` 미리보기 즉시 연동 / 미등록 시 `등록된 이미지 없음` 플레이스홀더 표시, `[이미지 업로드]`, `[이미지 변경]`, `[이미지 삭제]` 상태별 버튼 연동
   - [우선순위 & 문구] 영상 등록 시 영상 최우선 적용(미등록 시 이미지 적용), 제목 1·2(각 30자 2열) & 서브카피(80자 전체너비) 글자수 카운터 및 저장 연동

7. **WHY FLOW (`whyflow`)**:
   - 4대 핵심 강점 가치 카드 가로형 컴팩트 레이아웃 (제목 40%, 설명 60%)
   - 컴팩트 여백 적용 및 하단 일괄 저장 기능

8. **갤러리 관리 (`gallery`)**:
   - 카테고리별(프리다이빙, 입영, 수상구조사) 사진 목록 조회
   - 새 이미지 등록, 업로드/경로/대체 텍스트(ALT) 편집 및 삭제

9. **팝업 관리 (`popup`) [완료]**:
   - 상단 3대 실데이터 통계 카드 (전체 팝업, 노출중, 비노출 - 더미 상수 제거 및 실시간 집계)
   - 검색(제목+설명), 노출 상태 필터, 정렬(우선순위순 기본 / 최신순)
   - 목록 테이블 클릭 시 우측 실시간 팝업 목업 프리뷰 및 상세 메타 정보 테이블 즉시 연동
   - 불필요한 '노출 대상' UI 및 복제 기능 완전 제거, 표준 안전 삭제 모달(`openSafetyModal`) 적용
   - 상세 에디터: 제목(40자)/설명(80자) 글자수 카운터, 시작일/종료일 유효성 검증, 연결 링크(새 창 옵션), 하루 보지 않기 가로 flex row 배치 및 토글, 이미지 업로드/변경/삭제, 노출 순서 읽기 전용 뱃지(`N순위`) 안내
   - 목록 드래그 앤 드롭을 이용한 노출 순서 변경 및 `[✓ 순서 적용하기]` 버튼을 통한 LocalStorage 영구 저장 (F5 새로고침 후에도 순서 100% 유지)
   - 팝업 우선순위 중복 완전 차단 및 연속 번호(`1, 2, 3...`) 자동 정규화, 노출/비노출 토글 및 삭제 시 순위 자동 압축

10. **Footer 관리 (`footer`)**:
    - 기본 정보: 슬로건, 대표자명, 사업자등록번호, 주소, 전화번호, 이메일
    - SNS 채널 관리: 4개 기본 SNS(인스타그램, 블로그, 유튜브, 카카오톡) 사용 토글 및 링크 설정

11. **연동 서비스 (`links`)**:
    - 네이버 예약, 카카오톡 상담, 네이버 스마트 플레이스, 네이버 카페, 스마트스토어, 인스타그램, 유튜브, 블로그, 전화번호 외부 URL 일괄 관리

12. **SEO 설정 (`seo`)**:
    - 페이지 타이틀, 메타 설명(Description), OG(OpenGraph) 이미지 업로드 및 저장

---

## 3. 팝업 관리 및 홈페이지 연동 최종 완료 내역

### [1] 팝업 관리 및 홈페이지 연동 완료 항목
1. **관리자 팝업 기능 완벽 동작**:
   - 팝업 등록 / 수정 / 삭제 (표준 모달)
   - 공개 / 비공개 (스위치 토글)
   - 시작일 / 종료일 기간 제어
   - 팝업 이미지 업로드 / 변경 / 삭제
   - 연결 링크 (`#program` 내부 링크 및 새창 지원)
   - 오늘 하루 보지 않기 (24시간 만료)
   - 상단 통계 3종 실데이터 연동 (더미 상수 제거)
   - 노출 대상 UI 및 복제 기능 완전 정리
2. **우선순위 관리 및 영구 저장**:
   - 팝업 우선순위 중복 0% 및 연속 번호(`1, 2, 3...`) 자동 보장
   - 상세 수정 화면의 숫자 조절 UI 제거 (읽기 전용 배지로 변경)
   - 목록 드래그 앤 드롭을 통한 직관적인 순서 변경
   - `[✓ 순서 적용하기]` 클릭 시 LocalStorage(`flow_cms_popup`)에 영구 저장
   - **F5 새로고침 및 브라우저 재방문 시에도 변경된 순서 100% 영구 유지 확인**
3. **홈페이지 실제 팝업 노출 및 애니메이션 연동**:
   - 관리자 활성 팝업 데이터 1:1 로드
   - 다중 팝업 4초 자동 전환 및 호버(`mouseenter`) 일시정지, `mouseleave` 재개
   - 이전 / 다음 수동 이동 및 페이지 번호 인디케이터
   - **CSS Grid Stack 기반 크로스페이드(Cross-fade) 전환 애니메이션** (흰색 플래시/번쩍임 완전 제거)
   - 홈페이지 실제 노출 순서가 관리자 우선순위(1순위 ➔ 2순위 ➔ 3순위)와 1:1 완벽 일치

---

## 4. 대시보드 (`dashboard`) UI 개선 및 기능 연동 최종 완료 내역

### [1] 대시보드 완료 항목
1. **디자인 시안 1:1 반영 프리미엄 레이아웃**:
   - 상단 4대 통계 카드 (오늘/이번 주/전체 방문자, 페이지뷰): 실제 연동 전 값 0 고정 및 향후 GA API 수신 DOM ID(`statTodayVisitors` 등) 완비
   - 중단 좌측: 최근 수정 내역 5개 고정 표시 (아이콘, 작업 설명, 일시, 관리 메뉴명 태그)
   - 중단 우측: 빠른 작업 4개 카드 (2x2 그리드) + 연동 서비스 3종 칩 (네이버 예약, 카카오톡 상담, 스마트스토어)
   - 하단: 알림 및 공지 카드 (실제 활성 팝업 요약 뱃지, 팝업 제목, 노출 기간, D-Day 계산 뱃지)
   - 푸터: `© 2026 FLOW FREEDIVING. All rights reserved.` / `v1.0.0`
2. **라우팅 및 브라우저 History 동작 통일**:
   - **빠른 작업**: 대시보드 ➔ 추가 화면 직접 이동 ➔ 브라우저 뒤로가기 시 대시보드로 즉시 복귀
   - **최근 수정 내역**: 관리자명(`admin`) 대신 작업 관련 관리 메뉴명 태그 표시 ➔ 클릭 시 해당 관리 메뉴로 즉시 이동
   - **알림 및 공지**: 활성 팝업 카드 클릭 시 해당 팝업 관리/수정 화면으로 즉시 이동
3. **UI 간소화 및 컴팩트 레이아웃**:
   - 불필요한 전체보기, 접기, 펼치기, 모달 등 제거하여 안정적인 고정 뷰 완성
   - 시스템 상태 및 Danger Zone UI 대시보드 제거 (JS 백업/복원 로직은 100% 안전 보존)
   - 한 화면 중심 컴팩트 레이아웃 및 세로 스크롤 정상 지원

---

## 5. 교육과정 (`programs`) 상세 화면 UI 압축 및 기능 연동 최종 완료 내역

### [1] 교육과정 상세 화면 완료 항목
1. **상단 버튼 정리 및 액션바**:
   - 상세 진입 시 상단 헤더의 `홈페이지 보기` 버튼을 자동 숨김 처리하여 화면 중복 제거
   - 액션바 내 `[👁️ 미리보기]`, `[📄 교육과정 복제]`, `[💾 저장하기]`(파란색 Primary 버튼) 순서 배치 및 저장 연동
   - **`미리보기` 클릭 시 현재 수정 중인 교육과정의 실제 홈페이지 상세 페이지(모달)를 새 탭으로 즉시 열기(`index.html#program-{id}`) 연동**
2. **기본 정보 + 대표 이미지 (상단 2열 레이아웃)**:
   - 좌측: 교육 과정명, 카테고리, 소제목, 수강료, 핵심 강습 설명, 공개 여부 토글 2열 컴팩트 배치
   - 우측: 정사각형(`155×155px`, `object-fit: cover`) 대표 이미지 프리뷰 + 우측 슬림 세로 정렬(`140px`) `[이미지 변경]`, `[이미지 삭제]`, `권장 이미지: 1920×1080px (16:9) | JPG, PNG 지원` 안내
   - 양측 카드 전체 높이를 1:1 완벽 일치 및 컴팩트 레이아웃 유지
3. **교육 내용 (한 줄 4열 그리드)**:
   - `상세 커리큘럼`, `포함 사항`, `개인 준비물`, `주의사항` 4개 입력창의 높이를 `120px`로 동일하게 균일화
   - 제목에서 `(줄바꿈으로 구분)` 문구 제거 및 기본 내용을 스크롤 없이 한눈에 확인할 수 있는 최적 높이 확보
4. **판매 및 노출 설정 (통합 카드)**:
   - `스마트스토어 개별 링크` | `태그` 2열 통합 카드 구성
   - 태그 라벨 간소화(`태그`) 및 Tag Chip 추가/삭제 기능 유지
5. **교육과정 삭제 영역 간소화 및 여백 정돈**:
   - 기존의 큰 위험 박스, 테두리, 배경색, 설명 문구 완전 제거
   - 화면 우측 하단에 작고 깔끔한 단독 빨간색 `[🗑️ 교육과정 삭제]` 버튼 배치 (상단 카드와 10px 자연스러운 여백 확보)
   - 클릭 시 기존 안전 확인 모달 연동 및 삭제 로직 100% 보존
6. **목록 버튼 명칭 통일 및 전체 화면 최적화**:
   - 교육과정 목록 상단 버튼을 `+ 교육과정 추가`로 통일
   - 교육과정 상세 내 개별 SEO 설정 완전 삭제 (사이드바 전체 SEO 설정은 100% 보존)
   - 1920×1080 데스크톱 환경에서 모든 주요 편집 내용이 한 화면에 들어오는 컴팩트 구조 완성

---

## 6. FAQ (`faq`) 목록 개수 및 페이지네이션 기능 최종 완료 내역

### [1] FAQ 완료 항목
1. **FAQ 목록 개수 자동 계산 및 동적 표시**:
   - 상단 액션바 제목 옆 뱃지(`<span class="badge" id="faqCount">N</span>`)에 실제 등록된 FAQ 총 개수 실시간 반영
   - FAQ 등록, 수정, 삭제 후에도 자동으로 최신 개수 동기화
2. **10개 단위 페이지네이션 (Pagination)**:
   - 한 페이지당 최대 10개 항목 렌더링 (`FAQ_PAGE_SIZE = 10`)
   - **10개 이하일 경우**: 페이지네이션 버튼 영역 자동 숨김 처리
   - **11개 이상일 경우**: 테이블 하단 중앙에 깔끔한 페이지네이션 네비게이션 (`< 1 2 3 4 5 >`) 표시
3. **최대 5개 페이지 번호 슬라이딩 윈도우 지원**:
   - 페이지 수가 많아져도 무한정 늘어나지 않고 최대 5개 페이지 번호 슬라이딩 윈도우 유지
   - 예: 초반 `< 1 2 3 4 5 >`, 중간 `< 4 5 6 7 8 >`, 후반 `< 6 7 8 9 10 >`
   - 첫 페이지에서 `<` 이전 버튼 자동 비활성화, 마지막 페이지에서 `>` 다음 버튼 자동 비활성화
   - 현재 활성화된 페이지 번호는 파란색 Primary 스타일로 직관적 강조
4. **안전 확인 모달 기반 삭제 및 데이터 동기화**:
   - 인라인 삭제 버튼(`정말 삭제? | 취소`) 방식을 제거하고 교육과정과 동일한 **통합 확인 모달(`openSafetyModal`) 연동**
   - 모달 안내: 제목 `FAQ를 삭제하시겠습니까?`, 설명 `삭제한 FAQ는 복구할 수 없습니다.`, 버튼 `취소` | `삭제하기`(위험 빨간색)
   - 삭제 완료 즉시 FAQ 목록 재렌더링, 상단 `FAQ 목록 N` 개수 동적 갱신, 페이지네이션 재계산 및 LocalStorage 영구 저장 100% 정상 작동

### [2] 메인 홈페이지 FAQ 완료 항목
1. **최초 6개 기본 노출 (깔끔한 초기 화면)**:
   - 메인 홈페이지 로드 시 최대 6개의 FAQ만 기본 노출하여 페이지 세로 길이가 과도하게 길어지는 현상 방지
   - 등록된 FAQ가 6개 이하일 경우 더보기 버튼 숨김 처리
2. **6개씩 단계별 'FAQ 더보기' 단일 버튼 펼침**:
   - 등록된 FAQ가 7개 이상일 경우 하단 중앙에 라운드형 `[ FAQ 더보기 ↓ ]` 단독 버튼 표시
   - 클릭 시마다 6개씩 점진적으로 추가 노출 (예: 1~6 ➔ 1~12 ➔ 1~18)
   - 아직 표시할 FAQ가 남아있는 경우 `[ FAQ 더보기 ↓ ]` 단일 버튼 유지 (두 버튼이 동시에 나란히 노출되지 않음)
3. **전체 확인 완료 시 'FAQ 접기' 전환 및 스크롤 복귀**:
   - 모든 FAQ가 화면에 노출되면 `FAQ 더보기` 버튼은 사라지고 `[ FAQ 접기 ↑ ]` 버튼 1개만 단독 표시
   - `FAQ 접기 ↑` 클릭 시 초기 6개 상태로 접히며 `#faq` 섹션 상단으로 부드럽게 스크롤 이동, 버튼은 다시 `[ FAQ 더보기 ↓ ]`로 복원
4. **아코디언 기능 및 관리자 실데이터 100% 연동**:
   - 질문 클릭 시 답변이 부드럽게 열리고 닫히는 기존 아코디언 인터랙션 완벽 유지
   - 관리자 페이지에서 FAQ 추가/수정/삭제 시 메인 홈페이지에도 자동 실시간 반영

---

## 7. 후기 (`reviews`) 관리 화면 기능 최종 완료 내역

### [1] 후기 관리 화면 완료 항목
1. **후기 목록 개수 자동 계산 및 동적 표시**:
   - 상단 액션바 제목 옆 뱃지(`<span class="badge" id="reviewsCount">N</span>`)에 실제 등록된 후기 총 개수 실시간 반영
   - 후기 등록, 수정, 삭제 후에도 자동으로 최신 개수 동기화
2. **10개 단위 페이지네이션 (Pagination)**:
   - 한 페이지당 최대 10개 항목 렌더링 (`REVIEWS_PAGE_SIZE = 10`)
   - **10개 이하일 경우**: 페이지네이션 버튼 영역 자동 숨김 처리
   - **11개 이상일 경우**: 테이블 하단 중앙에 깔끔한 페이지네이션 네비게이션 (`< 1 2 3 4 5 >`) 표시 (최대 5개 슬라이딩 윈도우 지원)
3. **컴팩트 2열 상세 편집 폼 레이아웃**:
   - 1행: `수강생명` | `참가 과정` (2열 그리드)
   - 2행: `후기 평점` (1~5점 별점) | `작성 날짜` (2열 그리드)
   - 3행: `후기 내용` (적절한 높이의 컴팩트 textarea)
   - 데스크톱 환경에서 스크롤 부담 없이 한눈에 확인 및 수정 가능
4. **참가 과정 선택 방식 고도화**:
   - 기본 항목 `프리다이빙` 제공
   - 현재 관리자에 등록된 교육과정 목록 자동 동적 연동 (하드코딩 배제, '유스' 명칭 표준 준수)
   - `+ 직접 입력` 선택 시 과정명 직접 입력 인풋 동적 토글
   - 기존 후기 데이터와의 100% 호환성 보장
5. **작성 날짜 Date Picker 연동 및 데이터 포맷 표준화**:
   - 편집 폼에 `<input type="date">`를 적용하여 간편하게 날짜 지정 및 수정 가능
   - 데이터는 `YYYY-MM-DD` 표준 규격으로 저장되며, 목록 및 홈페이지에서는 기존 톤앤매너(`YYYY.MM.DD`)로 매끄럽게 호환 표시
6. **안전 확인 모달 기반 삭제 연동**:
   - 인라인 삭제 버튼 대신 통합 확인 모달(`openSafetyModal`) 연동
   - 모달 안내: 제목 `후기를 삭제하시겠습니까?`, 설명 `삭제한 후기는 복구할 수 없습니다.`, 버튼 `취소` | `삭제하기`(위험 빨간색)
   - 삭제 완료 즉시 후기 목록 재렌더링, 상단 뱃지 개수 자동 차감 및 페이지네이션 재계산

---

## 8. 강사 관리 (`instructors`) 목록 및 화면 개편 완료 내역

### [1] 강사 관리 목록 화면 완료 항목
1. **노출 상태 통일 (`메인 노출` 단일 토글)**:
   - 복잡했던 `공개 여부`와 `메인 노출` 이중 설정을 완전히 정리하고 **`메인 노출` (ON/OFF)** 단일 토글로 통일
   - `ON`: 메인 홈페이지 강사 영역에 노출
   - `OFF`: 홈페이지 강사 영역에서 숨김 (관리자 목록에는 유지되며 데이터는 안전하게 보존)
2. **드래그 앤 드롭 노출 순서 변경 및 순서 적용 버튼**:
   - 강사 목록 좌측에 `⠿` 드래그 핸들을 배치하여 마우스로 자유롭게 행 순서 재배치 가능
   - 드래그로 순서가 변경된 경우에만 하단에 **`순서 적용하기`** 버튼이 동적으로 표시
   - `순서 적용하기` 클릭 시 변경된 순서가 LocalStorage에 영구 저장되며, `강사 순서가 변경되었습니다.` 토스트 안내
   - 메인 홈페이지에도 관리자가 지정한 순서가 그대로 100% 동기화 반영
3. **불필요한 수정/삭제 버튼 제거 및 행 전체 클릭 시 상세 편집 이동**:
   - 교육과정 목록과 동일하게 테이블 행 내의 불필요한 개별 `수정`, `삭제` 버튼을 제거하여 UI를 간결하게 압축
   - 강사 행(`<tr>`) 또는 모바일 카드 클릭 시 해당 강사의 상세 편집 화면으로 즉시 이동
   - 드래그 핸들 및 `메인 노출` 토글 클릭 시에는 이벤트 버블링을 차단하여 상세 화면으로 오작동 이동하는 문제 완벽 방지
4. **강사 목록 10명 단위 페이지네이션 (Pagination)**:
   - 한 페이지당 최대 10명의 강사만 표시 (`INSTRUCTORS_PAGE_SIZE = 10`)
   - 10명 이하일 경우 페이지네이션 숨김, 11명 이상부터 `< 1 2 3 4 5 >` 슬라이딩 윈도우 페이지네이션 활성화
5. **상세 에디터 및 삭제 안전 확인 모달 연동**:
   - 상세 편집 폼 상단에 `메인 노출` 단일 토글 적용
   - `강사 프로필 삭제` 클릭 시 통합 확인 모달(`openSafetyModal`)을 통해 안전하게 삭제 처리 및 실시간 목록 갱신

### [2] 새 강사 등록 모드 및 기존 강사 수정 모드 완전 분리
1. **신규 등록 화면 초기화 및 빈 이미지 상태**:
   - `+ 새 강사 등록` 클릭 시 기존 강사 데이터나 이미지가 절대 복사/잔존하지 않도록 모든 입력값(`강사명`, `역할`, `교육 철학`, `BIO`, `자격증`, `경력`)을 빈값으로 완전 초기화
   - 이미지 영역: 사진이 없을 때 빈 회색 대시 플레이스홀더(`등록된 사진 없음`) 및 `[📤 이미지 업로드]` 버튼만 노출
2. **동적 화면 타이틀 분리**:
   - 신규 등록 시: **`새 강사 등록`** 타이틀 표시
   - 기존 강사 수정 시: **`강사 정보 편집`** 타이틀 표시
3. **신규 생성 및 수정 로직 격리**:
   - 신규 등록 시 고유 `id`(`instructor-${Date.now()}`) 및 `order`를 새로 부여하여 배열에 `push`, 기존 데이터 손상 방지
   - 저장 후 강사 목록 개수 뱃지 자동 갱신 및 F5 새로고침 후에도 영구 유지
   - `메인 노출` 기본값 `ON` 설정 및 관리자 사전 변경 가능

---

## 9. 메인 Hero (`hero`) 관리 화면 개선 완료 내역

1. **미디어 설정 좌우 2열 컴팩트 레이아웃 개편 (최대 높이 220px)**:
   - 데스크톱 화면에서 `배경 영상` 카드와 `배경 이미지` 카드를 **좌우 2열(1fr 1fr)**로 컴팩트하게 배치하고, 미리보기 영역의 최대 높이를 **약 220px (16:9 비율 유지, object-fit: cover)**로 축소
   - 카드가 화면을 과도하게 차지하지 않도록 하여 `미디어 설정 + 텍스트 문구 설정 + 저장 버튼`이 한 화면에 자연스럽게 들어오도록 최적화
   - 모바일 화면에서는 **1열**로 자동 스택되는 반응형 그리드(`hero-media-2col-grid`) 적용
   - 영상 및 이미지 미등록 시에도 동일한 16:9 컴팩트 플레이스홀더 박스 유지
2. **URL 직접 입력창 및 관련 UI 완전 제거**:
   - 관리자 화면에서 `영상 URL`, `이미지 URL` 직접 입력 텍스트박스 및 불필요한 보조 버튼을 완전히 제거
   - 순수 파일 업로드 방식으로 단순화하여 사용성 극대화 (업로드된 파일 URL은 내부적으로 자동 생성/저장)
3. **배경 영상 실시간 `<video>` 미리보기 및 상태별 버튼**:
   - 등록된 영상이 있을 때: 실제 비디오 플레이어(`<video controls>`)를 통해 관리자 화면에서 즉시 재생 확인 가능
   - 영상이 없을 때: 점선 플레이스홀더(`등록된 영상 없음`) 표시 및 `[📤 영상 업로드]` 단일 버튼 노출
   - 영상이 있을 때: `[🔄 영상 변경]`, `[🗑️ 영상 삭제]` 버튼 활성화
4. **배경 이미지 실시간 `<img>` 미리보기 및 상태별 버튼**:
   - 기존에 미리보기가 정상 표시되지 않던 오류를 해결하고 실제 등록된 이미지(`<img>`)가 즉시 렌더링되도록 수정
   - 이미지가 없을 때: 점선 플레이스홀더(`등록된 이미지 없음`) 표시 및 `[📤 이미지 업로드]` 단일 버튼 노출
   - 이미지가 있을 때: `[🔄 이미지 변경]`, `[🗑️ 이미지 삭제]` 버튼 활성화
5. **Hero 적용 우선순위 및 텍스트 문구 설정 유지**:
   - 배경 영상 등록 시 영상 최우선 적용, 영상 미등록 시 배경 이미지 자동 적용 로직 유지
   - 제목 1(30자) / 제목 2(30자) 2열 배치 및 서브카피(80자) 전체너비 배치 및 실시간 글자수 카운터 연동
   - 하단 `[💾 저장하기]` 버튼을 통한 안전한 LocalStorage 영구 저장
6. **홈페이지 Hero 비디오 자동재생 및 IndexedDB 대용량 영구 저장 연동**:
   - `DataService`에 IndexedDB + LocalStorage 이중 영구 저장 레이어를 도입하여, 브라우저 LocalStorage 5MB 용량 한계로 인한 대용량 영상 Base64 저장 누락 문제를 원천 해결 (F5 새로고침 후에도 영구 유지)
   - 프론트 `js/app.js`의 `renderHero`에서 영상 등록 시 `<video class="hero__video" autoplay muted loop playsinline>` 요소의 DOM 속성 및 `.play()` 트리거를 확실히 바인딩하여 모든 모바일/데스크톱 브라우저에서 배경 영상이 완벽하게 자동재생되도록 구현

---

## 10. WHY FLOW 관리 화면 목록형 개편 및 아이콘 기능 완전 제거 완료 내역

### [1] WHY FLOW 관리 화면 완료 항목
1. **목록형 UI 테이블 개편 (Table List View)**:
   - 모든 항목을 펼쳐놓던 구형 방식을 폐지하고 `| 순서(⠿) | 제목 | 설명(1줄 말줄임) | 노출(ON/OFF) |` 표준 콤팩트 테이블로 개편
   - 상단 액션바에 총 항목 수 뱃지(`<span class="badge" id="whyflowCount">N</span>`) 및 `[+ 항목 추가]` 버튼 배치
2. **아이콘 기능 완전 제거**:
   - 상세 편집 화면 및 신규 등록 화면에서 `아이콘` 선택 UI를 완전히 제거
   - 메인 홈페이지 WHY FLOW 카드에서도 아이콘 요소를 제거하여 제목과 설명 중심의 깔끔한 카드로 렌더링
   - 기존 저장 데이터에 아이콘 값이 있더라도 화면에 전혀 노출되지 않도록 처리
3. **노출 상태 통일 (단일 토글)**:
   - `ON`: 메인 홈페이지 WHY FLOW 영역에 노출
   - `OFF`: 메인 홈페이지 영역에서 숨김 (관리자 목록에는 유지되며 데이터는 안전하게 보존)
4. **드래그 앤 드롭 순서 변경 및 순서 적용 버튼**:
   - `⠿` 드래그 핸들로 항목 순서를 마우스로 자유롭게 재배치 가능
   - 순서 변경 시 `[순서 적용하기]` 버튼이 동적으로 표시되며, 클릭 시 LocalStorage 영구 저장 및 토스트 알림 제공
5. **행 클릭 시 상세 편집 화면 진입 및 안전 삭제 연동**:
   - 행 클릭 시 상세 편집 화면으로 전환 (드래그 핸들 및 노출 토글 클릭 시 이벤트 버블링 차단)
   - 제목, 설명, 노출 상태, `[취소]`, `[저장하기]`, `[항목 삭제]`(통합 안전 확인 모달 `openSafetyModal` 연동) 제공
6. **상단 공통 문구(섹션 제목, 섹션 설명) 관리 카드 추가**:
   - WHY FLOW 목록 화면 상단에 `[상단 문구 설정]` 카드 배치 (`섹션 제목`, `섹션 설명`, `[저장하기]` 버튼)
   - 저장 시 `adminData.whyFlow.title`과 `subtitle`이 LocalStorage/IndexedDB에 즉시 영구 저장되며 홈페이지(`#about`) 영역과 1:1 실시간 동기화
   - F5 새로고침 후에도 저장된 값이 그대로 유지됨
7. **노출 카드 개수 기반 자동 중앙 정렬 (Auto-Center Alignment)**:
   - 고정 4열 Grid 대신 `display: flex; flex-wrap: wrap; justify-content: center;` 반응형 레이아웃 적용
   - 노출 항목 개수(1개, 2개, 3개, 4개)에 따라 우측 빈 공간 없이 전체 섹션의 정중앙을 기준으로 카드가 자동 정렬됨
   - 카드의 기존 크기, 패딩, 보더 및 모바일 반응형 1열 카드 정렬 완벽 유지

---

## 11. 갤러리 관리 (`gallery`) 상세 화면 개편 및 기능 완료 내역

### [1] 갤러리 관리 완료 항목
1. **상세 화면 대형 이미지 미리보기 및 원클릭 업로드/변경**:
   - 상세 화면 상단에 등록된 이미지의 고해상도 대형 미리보기(`max-height: 360px`, `object-fit: contain`) 제공
   - 이미지가 없을 때: `[📤 이미지 업로드]` 버튼 및 점선 플레이스홀더 표시
   - 이미지가 있을 때: `[🔄 이미지 변경]` 버튼으로 손쉽게 이미지 교체 지원
2. **이미지 경로 URL 직접 입력란 완전 제거**:
   - 관리자가 긴 Base64나 URL 경로를 직접 보거나 수정할 필요 없이 백그라운드에서 파일 업로드 시 자동 할당 및 관리
   - 기존 저장 데이터 구조(`src`, `alt`, `category`, `visible`) 100% 호환 보존
3. **카테고리 3종 통일 표준화**:
   - `프리다이빙` (`freediving`), `수영` (`swimming`/`eggyeong`), `기타` (`etc`/`youth`/`kids`/`lifeguard`) 3개 카테고리로 통합
   - 관리자 등록/수정 화면 및 메인 홈페이지 갤러리 탭/필터 모두 3개 카테고리로 1:1 완벽 일치 연동
4. **이미지 설명 필드 도입 (ALT 텍스트)**:
   - 생소한 ALT 용어 대신 관리자 친화적인 `이미지 설명` 레이블 적용 (예: `풀장에서 프리다이빙 연습`)
   - 웹 접근성 및 SEO 표준 ALT 태그로 1:1 자동 바인딩
5. **홈페이지 노출 ON/OFF 토글 추가**:
   - 노출 토글(`ON`/`OFF`)을 통해 개별 이미지를 손쉽게 공개/비공개 전환
   - 메인 홈페이지 갤러리 및 라이트박스에서 `visible !== false`인 활성 이미지만 깔끔하게 렌더링
6. **하단 액션 버튼 재배치 및 안전 삭제 모달 연동**:
   - 좌측: `[취소]`(Ghost) / `[저장하기]`(Primary)
   - 우측: `[이미지 삭제]` (신규 추가 시 숨김, 기존 이미지 수정 시 `openSafetyModal` 확인 모달 연동)
7. **갤러리 목록 테이블 뷰 및 행 클릭 상세 이동**:
   - 목록 테이블에서 불필요한 개별 `수정`, `삭제` 버튼을 제거하고, 행 전체(`<tr>`) 클릭 시 해당 이미지의 상세 편집 화면으로 직접 이동 (삭제는 상세 화면에서만 안전하게 처리)
8. **상단 카테고리 필터 탭 (`[전체] [프리다이빙] [수영] [기타]`)**:
   - 갤러리 목록 상단에 카테고리 필터 그룹 배치, 선택된 카테고리의 이미지만 즉시 필터링 조회
   - 필터 전환 시 1페이지로 자동 리셋
9. **목록 보기 & 썸네일 보기 듀얼 뷰 모드 전환 (`[목록 보기] [썸네일 보기]`)**:
   - **목록 보기**: `| 미리보기 | 이미지 설명 | 카테고리 | 노출 상태 |` 정돈된 테이블 뷰
   - **썸네일 보기**: 썸네일 사진, 이미지 설명, 카테고리 및 노출 뱃지가 포함된 직관적이고 컴팩트한 카드형 그리드 뷰
   - 카드/행 클릭 시 동일하게 상세 편집 화면으로 이동
10. **20개 단위 페이지네이션 (Pagination)**:
    - 한 페이지당 최대 20개 이미지 표시 (`GALLERY_PAGE_SIZE = 20`)
    - 20개 초과 시 `< 1 2 3 4 5 >` 슬라이딩 윈도우 페이지네이션 표시, 20개 이하 시 자동 숨김

---

## 12. 갤러리 별도 전체 페이지 (`gallery.html`) 분리 및 연동 완료 내역

### [1] 주요 구현 항목
1. **별도 갤러리 전체 페이지 신설 (`gallery.html` & `js/gallery-page.js`)**:
   - 관리자 갤러리 데이터(`DataService.getGallery()`)와 단일 원천(Single Source of Truth)으로 연동
   - 관리자에서 `노출 (ON)` 상태인 모든 이미지를 카테고리별로 조회할 수 있는 전용 갤러리 페이지 구축
   - 브랜드 디자인 시스템 톤앤매너(Pretendard, Noto Sans KR, Glassmorphism 헤더, 반응형 레이아웃) 100% 일치
2. **상단 서브 히어로 및 카테고리 필터 연동**:
   - 서브 히어로 헤더: `GALLERY` / `FLOW와 함께한 순간들`
   - 카테고리 필터 탭: `[전체] [프리다이빙] [수영] [기타]` (선택 시 해당 카테고리 사진만 부드럽게 필터링)
3. **반응형 갤러리 그리드 및 풀스크린 라이트박스**:
   - PC: 3~4열 반응형 그리드, 모바일(≤768px): 2열 그리드 (`repeat(2, 1fr)`) 완벽 대응
   - 이미지 호버 시 줌 애니메이션 및 확대 아이콘 오버레이 제공
   - 이미지 클릭 시 전체 화면 라이트박스 팝업(이전/다음 네비게이션, 닫기, 키보드 방향키 및 ESC 제어) 연동
4. **메인 홈페이지 GALLERY 섹션 최적화 (`index.html`)**:
   - 메인 홈페이지에서는 카테고리 탭 버튼을 제거하고, 관리자 `노출 ON` 이미지 중 앞쪽 최대 8개의 대표 이미지만 깔끔하게 미리보기로 렌더링
   - 사진 그리드 시작 전 우측 상단에 심플한 텍스트 링크 형태의 `전체보기 →` (`.gallery-more-link`) 배치 (사진 하단 버튼 제거)
   - `전체보기 →` 클릭 시 기존 별도 `gallery.html` 페이지로 원클릭 즉시 이동
   - PC 4열, 태블릿 3열, 모바일 2열(`repeat(2, 1fr)`) 반응형 그리드 정렬 및 라이트박스 확대보기 유지
5. **단일 데이터베이스 및 CRUD 100% 호환 보존**:
   - 관리자에서 이미지 등록/수정/삭제/노출 설정 시 메인 페이지와 전체 갤러리 페이지 양쪽에 실시간 동시 반영
   - 기존의 모든 갤러리 기능과 데이터 구조가 완벽히 보존됨
   - 상단 네비게이션 `GALLERY` 메뉴 및 우측 상단 `전체보기 →` 링크 모두 `gallery.html`로 1:1 완벽 연결

---

## 13. GALLERY 영상 기능 확장 (1~4단계: 데이터 구조, 관리자 UI, 영상 업로드 & 관리자 최종 완성)
1. **기존 카테고리 100% 유지**:
   - `프리다이빙` (`freediving`), `수영` (`swimming`), `기타` (`etc`) 3개 카테고리 체계 그대로 유지 (`영상`을 카테고리로 혼용하지 않고 완전히 분리)
2. **미디어 종류(`mediaType`) 및 영상/썸네일 필드 구조**:
   - 사진: `mediaType: 'image'`, `src: 이미지URL`, `alt: 설명`
   - 영상: `mediaType: 'video'`, `videoUrl: 영상URL`, `thumbnailUrl: 포스터URL`, `src: 포스터URL` (기존 이미지 뷰어 컴포넌트 100% 호환 보장)
3. **Cloudinary 영상 업로드 및 최적화 ([js/data-service.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/data-service.js))**:
   - `DataService.uploadFile(file, 'video')`: 비디오 리소스 타입 엔드포인트 자동 분기 연동
   - 업로드 중 실시간 진행/대기 상태 메시지 및 파일 용량 표시
   - 업로드 실패 시 명확한 오류 알림 및 빈 URL 저장 방지
4. **영상 썸네일 대표 프레임 자동 생성 ([js/admin.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/admin.js))**:
   - Cloudinary 업로드 시 Cloudinary 동적 포스터 트랜스포메이션(`/so_0,q_auto,f_jpg/`)을 통해 대표 프레임 썸네일 자동 추출
   - 로컬 테스트/오프라인 환경에서도 `<canvas>` 브라우저 프레임 캡처를 통해 썸네일 자동 생성 보장
   - 사용자가 별도로 썸네일 이미지를 수동 업로드할 필요가 없음
5. **관리자 목록/썸네일 뷰 최종 정리 및 사진/영상 배지 구분 ([js/admin.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/admin.js))**:
   - 테이블 및 썸네일 그리드에 `[사진]`, `[영상]` 배지 및 영상 썸네일 위 `▶` 오버레이 표시
   - 영상 썸네일이 목록/그리드에 100% 정상 출력되어 빈 화면 방지
   - 목록 행 및 썸네일 카드 전체 클릭 시 기존 상세/수정 화면 즉시 진입 정상화
   - 드래그 핸들(`⋮⋮`) 등 독립 요소에 `event.stopPropagation()` 적용으로 버블링 간섭 완벽 차단
6. **사진/영상 통합 드래그 앤 드롭 순서 변경 및 `순서 적용하기` 완벽 지원**:
   - 사진과 영상이 섞여 있어도 행/카드를 마우스로 드래그하여 순서 자유 재배치 가능
   - 순서 변경 시 하단 `순서 적용하기` 버튼이 나타나며, 클릭 시 IndexedDB/LocalStorage에 영구 저장 (F5 새로고침 후에도 유지)
7. **기존 데이터 및 CRUD 기능 100% 보존**:
   - 기존 저장 데이터 중 `mediaType`이 없는 항목은 영구적으로 `사진`으로 안전하게 호환
   - 사진/영상 통합 등록, 수정, 삭제, 노출 ON/OFF 토글 완벽 작동
8. **홈페이지 전체 갤러리 페이지([gallery.html](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/gallery.html), [js/gallery-page.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/gallery-page.js)) 영상 필터 탭 추가 (5-1단계)**:
   - 필터 탭 순서: `전체 | 프리다이빙 | 수영 | 영상 | 기타`
   - `전체`: 모든 사진 + 영상 노출
   - `프리다이빙`: 프리다이빙 카테고리의 모든 사진 + 영상 노출
   - `수영`: 수영 카테고리의 모든 사진 + 영상 노출
   - `영상`: 카테고리와 무관하게 `mediaType === 'video'`인 모든 영상만 노출
   - `기타`: 기타 카테고리의 모든 사진 + 영상 노출
   - 기존 버튼 스타일, 네이비 액티브 디자인, 여백 및 폰트 100% 동일 유지
9. **홈페이지 전체 갤러리 페이지 영상 카드 표시 및 라이트박스 비디오 재생 (5-2단계)**:
   - 영상 카드: `thumbnailUrl` 썸네일 노출 및 중앙 미니멀 `▶` 재생 아이콘 배지 오버레이
   - 사진 카드: 기존 확대 아이콘 오버레이 및 이미지 확대보기 유지
   - 영상 클릭 시: 라이트박스 내에서 HTML5 `<video controls playsinline>` 플레이어로 동영상 재생 (재생, 일시정지, 탐색바, 음량, 전체화면 지원)
   - 영상 닫기 / 이전·다음 이동 시: 영상 일시정지 및 `src` 리셋으로 소리 중복 및 백그라운드 재생 완벽 차단
   - 닫기 버튼, 배경 클릭, ESC 키 종료 동작 100% 지원
   - 초기 페이지 로드 시 불필요한 자동재생 없이 사용자 클릭 시에만 재생 시작
10. **메인 페이지 GALLERY 영상 표시 및 비디오 재생 연동 (5-3단계)**:
   - 메인 GALLERY 기존 그리드 레이아웃, 카드 크기(1:1), 반응형, `전체보기 →` 링크(그리드 우측 상단) 100% 보존
   - 관리자 노출 순서 및 최대 8개 노출 제한, 비공개(`visible: false`) 필터링 유지
   - 영상 카드: `thumbnailUrl` 썸네일 + 중앙 `▶` 재생 아이콘 배지 표시 (초기 자동재생 없이 썸네일만 렌더링)
   - 영상 클릭 시: 메인 라이트박스에서 HTML5 `<video>` 플레이어로 영상 재생, 닫기 시 즉시 소리/재생 중지
   - 사진 클릭 시: 기존 사진 확대보기 라이트박스 정상 작동
11. **Firebase 백엔드 구축 — 8-1 기본 연결 기반 구축 (완료)**:
   - [js/config.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/config.js): Firebase `flowfreediving-e79bc` 실제 설정값 반영 및 `isFirebaseConfigured()` 활성화 (`true`)
   - [js/firebase-service.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/firebase-service.js): Firebase SDK v10 Modular 기반 App, Authentication(`auth`), Cloud Firestore(`db`) 정상 초기화 완료
   - 기존 IndexedDB / LocalStorage / Mock Data 저장 방식 100% 정상 유지 (기존 웹사이트 및 관리자 기능 무중단 보존)
12. **Firebase 백엔드 구축 — 8-2 관리자 로그인 연동 (완료)**:
   - [admin-login.html](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/admin-login.html): 기존 하드코딩된 임시 계정 로직 제거, Firebase Authentication `signInWithEmailAndPassword` 연동 완료
   - 한국어 친화적 에러 메시지(이메일 형식, 비밀번호 불일치, 계정 비활성화, 시도 초과 등) 처리 및 로딩 상태 제어
   - [admin.html](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/admin.html) / [js/admin.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/admin.js): `onAdminAuthStateChanged` 기반 비인가자 접근 차단 및 `admin-login.html` 강제 리다이렉트 보호 적용
   - 로그아웃 버튼과 Firebase `signOut` 연동 (세션 종료 및 로그인 페이지 이동)
   - 소스코드 내 계정/비밀번호 하드코딩 일체 없음
   - 기존 관리자 CRUD 및 홈페이지 기능 100% 정상 유지
13. **Firebase 백엔드 구축 — 8-3 Firestore 데이터 저장 및 CRUD 연동 (완료)**:
   - [js/data-service.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/data-service.js): 12개 전체 섹션(hero, whyFlow, programs, instructors, reviews, gallery, faq, footer, links, seo, popup, courseFinder, cta) Cloud Firestore `content` 컬렉션 및 서브컬렉션(`content/{key}/items/{itemId}`) 연동 완료
   - **페이로드 용량 초과(`payload size exceeds`) 해결**: 대용량 Base64 데이터 자동 감지 및 Firestore 전송 제외(`sanitizeForFirestore`) → 텍스트, 메타데이터, URL, 카테고리, 순서, 공개여부만 안전 저장 (문서당 수백 바이트~수십 KB로 극도로 경량화)
   - **로컬 미디어 완벽 보존(`mergeLocalMedia`)**: Firestore 메타데이터와 로컬 IndexedDB/LocalStorage 미디어 데이터를 자동 병합하여 화면 렌더링 손실 방지
   - **조회/저장/삭제 흐름**: Firestore 실시간 읽기/쓰기 + IndexedDB/LocalStorage 로컬 캐시 및 이중 백업 보존
   - **자동 시딩 및 데이터 보호**: Firestore 최초 연결 시 기존 로컬 데이터 자동 시딩 지원 및 로컬 백업 영구 유지
   - **권한 분기**: 일반 방문자는 공개 데이터 읽기 가능(`allow read: if true`), 관리자만 쓰기/수정/삭제 가능(`allow write: if request.auth != null`)
   - **홈페이지 실시간 반영**: 관리자에서 데이터 수정 시 `index.html` 및 `gallery.html`에 실시간 즉시 반영
14. **Firebase 백엔드 구축 — 8-4 Cloudinary 이미지·영상 업로드 연동 (완료)**:
   - [js/config.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/config.js): Cloudinary Unsigned Upload Preset 기반 환경설정(`ov24dbs2` / `flow_unsigned_preset`) 및 `isCloudinaryConfigured()` 유효성 검사기 구축
   - [js/data-service.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/data-service.js): `DataService.uploadFile`를 통해 이미지 및 영상(MP4, MOV, WebM)의 Cloudinary 실제 업로드, 원본 HTTPS URL(`secure_url`), `public_id`, 영상 자동 포스터 썸네일(`thumbnail_url`), 미디어 메타데이터(크기, 포맷, 길이) 완벽 반환
   - [js/admin.js](file:///d:/안티그래피티/26-08-02_플로우%20-%20복사본/flow_backup_14_관리자재작업시작/js/admin.js): 갤러리 사진/영상, 교육과정 대표 이미지, 강사 프로필 사진, 팝업 이미지 업로드 핸들러 연동
   - **보안 및 경량화**: 프론트엔드 코드에 API Secret 노출 없음(Unsigned Preset 사용), Firestore에는 바이너리/Base64 없이 가벼운 URL과 메타데이터만 저장
   - **기존 데이터 안전 보존**: 기존 로컬 미디어는 삭제되지 않고 fallback으로 100% 안전 유지
15. **최종 검증 및 Vercel Production 배포 (완료)**:
   - **메인 홈페이지 (`index.html`) & 전체 갤러리 (`gallery.html`)**: 정상 렌더링, 갤러리 사진/동영상 필터 및 라이트박스 비디오 플레이어/자동 정지 정상 작동
   - **관리자 인증 (`admin-login.html`) & 보안 가드**: Firebase Authentication 로그인/로그아웃 연동, 미인증 사용자 접근 차단(리다이렉트) 완벽 작동
   - **Firestore CRUD & Cloudinary 업로드**: 12개 패널 데이터 실시간 읽기/저장/삭제, 미디어 업로드 및 자동 썸네일 생성 정상, F5 새로고침 후 데이터 완벽 유지
   - **반응형 & 크로스 브라우징**: 모바일 드로어, 태블릿, 데스크톱 레이아웃 100% 정상 작동, 브라우저 콘솔 에러 없음

---

## 14. 현재 데이터 저장 아키텍처 (Current Storage Architecture)

* **관리자 인증 (Auth)**: Firebase Authentication (Email / Password)
* **텍스트 / 설정 / CRUD 데이터**: Cloud Firestore (`content` 컬렉션 및 개별 서브컬렉션 문서)
* **이미지 / 영상 원본 미디어**: Cloudinary (Unsigned Upload Preset `flow_unsigned_preset`)
* **Firestore 저장 정보**: Cloudinary HTTPS URL 및 미디어 메타데이터 (제목, 설명, 카테고리, 순서, 공개여부 등)
* **로컬 캐시 및 백업**: IndexedDB (`flow_cms_db`) 및 LocalStorage에 이중 보존

---

## 15. 최종 운영 및 유지보수 상태 (Production Status)

* **Vercel Production 환경**: 정상 배포 및 라이브 서비스 가동 중
* **잔여 결함 및 콘솔 에러**: 0건 (모든 핵심 기능 및 보안 가드 정상 작동 확인 완료)


